# template-npm-static-site

This is for the building a static site using NPM as a task runner.

## Prerequisites

Install require node modules using:

```
npm install
```

## To start run:

```
npm start
```